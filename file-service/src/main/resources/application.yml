# file-service 配置（骨架，Step 7 迁入完整配置）
spring:
  application:
    name: scx-file
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: "optional:file:.env[.properties]"

server:
  port: ${FILE_PORT:3004}
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    com.scx.backend: DEBUG
